= form_for([@question, @commentable]) do |f|
  - if @commentable.errors.any?
    #error_explanation
      %h2
        = pluralize(@commentable.errors.count, "error")
        prohibited this commentable from being saved:
      %ul
        - @commentable.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label :comments
    %br/
    = f.text_area :comment, cols: '60', rows: '10'
    - if params[:solution_id].present?
      = hidden_field_tag :solution_id, params[:solution_id]
  .actions
    = f.submit
